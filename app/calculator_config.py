"""Configuration management for the calculator application.

Loads and validates application settings from environment variables using
python-dotenv (e.g. history file location, auto-save behavior, precision).
"""
